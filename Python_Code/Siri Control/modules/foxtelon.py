#You can import any modules required here
#from websocket import create_connection

#This is name of the module - it can be anything you want
moduleName = "foxtel_on"

#These are the words you must say for this module to be executed
commandWords = ["foxtel","on"]

#This is the main function which will be execute when the above command words are said
def execute(command):
    #print("\n")
    print("send command to Foxtel turn it on- foxtelon.py")
    #print("\n")
    #from websocket import create_connection
    #ws = create_connection("ws://192.168.1.128:4054/websocket")
    #ws = create_connection("ws://localhost:8080/websocket")
    #print "Sending 'Switch to Foxtel'..."
    #ws.send("Foxtel On")
    #print "Sent"
    #print "Receiving..."
    #result =  ws.recv()
    #print "Received '%s'" % result
    #ws.close()

